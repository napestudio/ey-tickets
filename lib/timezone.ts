/**
 * Default timezone for date calculations.
 * All sales hour/day groupings use this timezone.
 * If multi-timezone support is needed in the future, this can be
 * replaced with a per-Producer value from ProducerConfiguration.
 */
export const DEFAULT_TIMEZONE = "America/Argentina/Buenos_Aires";
