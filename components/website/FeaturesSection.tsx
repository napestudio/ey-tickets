import SectionTitle from "./ui/SectionTitle";

export default function FeaturesSection() {
  return (
    <section className="min-h-screen py-24 bg-linear-to-t from-ey-dark to-ey-turquoise-darker to-80%">
      <div className="container mx-auto">
        <SectionTitle as="h2">FUNCIONES</SectionTitle>
      </div>
    </section>
  );
}
